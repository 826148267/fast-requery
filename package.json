{
  "name": "fast-requery",
  "version": "1.0.0",
  "description": "Chrome extension for URL parameter management",
  "main": "popup.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --config .jest.config.js --watch",
    "build": "chmod +x ./scripts/build.sh && ./scripts/build.sh"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^24.0.0"
  }
}
